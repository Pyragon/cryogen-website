extend ../default

block head
    +includeScript('/utils/tabs')
    script.
        $(document).ready(() => {

            buildPage('account', {
                'player-reports': 'Player Reports',
                'bug-reports': 'Bug Reports',
                'appeals': 'Appeals',
                'recoveries': 'Recoveries'
            }, '!{active}');

        });

block content
    include ../utils/tabs