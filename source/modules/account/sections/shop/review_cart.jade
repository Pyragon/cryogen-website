div.noty_message
  span.noty_text
  p.small If you wish to fully remove an item, you will need to cancel this dialog and remove it from the webpage.
  div.cart-review
    ul
      for id in cart.keySet()
        -var amount = cart.get(id);
        -var item = items.get(utils.parseInt(id));
        li.cart-item(data-id=id)
          span.item-info= amount+' x '+item.getName()+' '
            span.fa.fa-minus
            span  
            span.fa.fa-plus
  p.small If you click confirm, you will be redirected to the paypal payment page.
  div.noty_close
