span.post-spoiler.spoiled(title=(user == null || user.getPostCount() < count ? 'You need at least '+count+' post count to read this.' : ''))= user == null || user.getPostCount() < count ? 'You need at least '+count+' post count to read this.' : post